заменить(f,x,a)=
  если null(x) то '()
  иначе если f(car(x)) то cons(a, заменить(f,cdr(x),a))
  иначе cons(car(x), заменить(f,cdr(x),a));
  
// заменить(atom, '((A) B (C) D E (F)), 'Z)
// заменить(atom, '(), 'Z)
// заменить(atom, '(A B C D F), 'AAA)
// заменить(atom, '((A) (B) (C)), 'Z)
