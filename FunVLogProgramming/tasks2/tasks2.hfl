заменить(f,x,a)=
  если null(x) то '()
  иначе {пусть t = заменить(f,cdr(x),a)
       в если f(car(x))
            то cons(a,t)
            иначе cons(car(x),t)};

заменить(atom, '((A) B (C) D E (F)), 'Z)